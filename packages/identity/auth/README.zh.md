# @deepseek-ai/dsh-auth

[English](README.md) | 简体中文 | [繁體中文](README.zh-tw.md)

经过验证的外部身份和请求作用域身份验证的 Cordis 约定。DSH 只接收已验证用户断言；凭证收集、账户创建和会话签发属于独立部署的身份验证服务。

## 模型体验

无，因为消费方使用请求身份进行访问控制，而不会将其加入模型输入。

#### KV Cache 影响

无；该包不组装模型提供方请求。

## 已知限制与延后工作

- 授权角色和账户关联元数据尚未包含在 `AuthenticatedUser` 中。
